# PostgreSQL connection string used by Prisma at runtime.
# Matches the credentials defined in /docker-compose.yml at the monorepo root
# (POSTGRES_USER=vivaidea, POSTGRES_PASSWORD=vivaidea, POSTGRES_DB=vivaidea).
DATABASE_URL="postgresql://vivaidea:vivaidea@localhost:5432/vivaidea?schema=public"
